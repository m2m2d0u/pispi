services:

  postgres:
    image: postgres:17
    container_name: pispi-postgres
    environment:
      POSTGRES_DB: pispi
      POSTGRES_USER: pispi
      POSTGRES_PASSWORD: pispi
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pispi -d pispi"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: pispi-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  kafka:
    image: confluentinc/cp-kafka:7.6.0
    container_name: pispi-kafka
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
    ports:
      - "9092:9092"
    volumes:
      - kafka_data:/var/lib/kafka/data
    healthcheck:
      test: ["CMD-SHELL", "kafka-broker-api-versions --bootstrap-server localhost:9092"]
      interval: 10s
      timeout: 5s
      retries: 5

#  app:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    container_name: pispi-app
#    depends_on:
#      postgres:
#        condition: service_healthy
#      redis:
#        condition: service_healthy
#      kafka:
#        condition: service_healthy
#    environment:
#      DB_HOST: postgres
#      DB_PORT: 5432
#      DB_NAME: pispi
#      DB_USERNAME: pispi
#      DB_PASSWORD: pispi
#      REDIS_HOST: redis
#      REDIS_PORT: 6379
#      REDIS_PASSWORD: ""
#      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
#      PISPI_CODE_MEMBRE: ${PISPI_CODE_MEMBRE:-CIF001}
#      PISPI_AIP_BASE_URL: ${PISPI_AIP_BASE_URL:-https://aip-sandbox.bceao.int}
#      PISPI_AIP_API_KEY: ${PISPI_AIP_API_KEY:-CHANGE_ME}
#      PISPI_API_VERSION: ${PISPI_API_VERSION:-1}
#      PISPI_WEBHOOK_URL: ${PISPI_WEBHOOK_URL:-https://sycapay-backend.local/api/pi-spi/webhook}
#      PISPI_MTLS_KEYSTORE_PATH: ${PISPI_MTLS_KEYSTORE_PATH:-classpath:keystore.p12}
#      PISPI_MTLS_KEYSTORE_PASSWORD: ${PISPI_MTLS_KEYSTORE_PASSWORD:-changeit}
#      PISPI_MTLS_TRUSTSTORE_PATH: ${PISPI_MTLS_TRUSTSTORE_PATH:-classpath:truststore.p12}
#      PISPI_MTLS_TRUSTSTORE_PASSWORD: ${PISPI_MTLS_TRUSTSTORE_PASSWORD:-changeit}
#    ports:
#      - "8080:8080"

volumes:
  postgres_data:
  redis_data:
  kafka_data:

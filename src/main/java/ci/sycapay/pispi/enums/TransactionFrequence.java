package ci.sycapay.pispi.enums;

/**
 * Frequency for scheduled / subscription transfers per the BCEAO remote spec
 * ({@code TransactionFrequence}): {@code J}ournalier, {@code S}emaine,
 * {@code M}ensuel, {@code A}nnuel.
 */
public enum TransactionFrequence {
    J, S, M, A
}
